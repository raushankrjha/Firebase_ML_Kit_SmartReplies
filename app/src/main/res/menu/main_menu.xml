<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_swap_users"
          android:title="@string/swap_users"
          android:icon="@drawable/ic_swap"
          app:showAsAction="always"/>

    <item android:id="@+id/action_clear_chat_history"
          android:title="@string/clear_chat_history"
          android:icon="@drawable/ic_clear_all"
          app:showAsAction="ifRoom"/>
</menu>